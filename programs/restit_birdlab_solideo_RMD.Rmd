---
title: "restit_birdlab_solideo"
author: "Heloise Jeux"
date: "`r Sys.Date()`"
output:
  html_document:
    toc: true
    toc_float: true
---


```{r setup, include = FALSE}
knitr::opts_chunk$set(echo = FALSE, 
                      cache = FALSE,
                      message = FALSE, 
                      warning = FALSE)
```

<br>

## Carte simplifiée des collections

/!\ AVANT QUELLE DATE EST-CE LE BAZAR POUR LES COORD DE MANGEOIRES ?  Ex: ici la mangeoire "Mangeoires" est répliquée plusieurs fois.
```{r carto_simple}
carte_collections
```

<br>

# **EN BREF...** {.tabset .tabset-pills}

<br>
<font size= "5" >
...la biodiversité que vous avez observée
</font>
<br>


Au total, `r nb_sp_tot_local` espèces d'oiseaux ont été observées sur votre site sur `r nb_mangeoire_local` mangeoires. Dans votre référentiel, `r nb_sp_tot_ref` espèces ont été observées. 
.\n

Nombre total d'espèces observées sur votre site parmi la liste des 30 espèces sur la saison en cours, ou si celle si est finie, lors de la dernière saison : 
```{r }
jauge_saison_en_cours
```

Nombre total d'espèces observées sur votre site parmi la liste des 30 espèces : 
```{r }
jauge_total
```



En moyenne sur les mangeoires de votre site, vous pouvez espérer voir environ `r round(nb_moy_esp_partie_local)` espèces. Dans le référentiel, c'est en moyenne `r round(nb_moy_esp_partie_ref)` espèces qui sont observées par parties.









Fréquences d'observation des espèces : 
Ce graphe décrit les probabilités que vous avez d'observer les espèces sur votre site.
```{r }
func_plot_freq_combi()
```

Idées d'indicateurs supplémentaires : 
<br>
- temps moyen par espèce passé à la mangeoire 
<br>
- réseau d'association : espèces présentes simultanément aux mangeoires ? à la même mangeoire ? 
<br>
- phénologie des espèces au cours de la saison ? au cours de la journée en combinant les observations ? 
<br>
<br>

```{r }
func_test_reseau_asso()
```



<br>
<font size= "5" >
 ...la participation sur votre site 
</font>
<br>

Nombre de mangeoires sur le site : `r nb_mangeoire_local`, de type : `r type_mangeoire`.
<br>

Nombre total de participants sur le site : `r ntot_participant`

<br>

Evolution du nombre de participants sur votre site : 

```{r}
plot_evol_participant
```

<br>

Nombre de parties jouées localement : `r ntot_partie`
```{r}
plot_evol_participation
```

<br>

Nombre de gros participants et grosses participantes sur votre site (plus de 20 collections, au total, depuis le début des relevés) : `r participant_gros` 
<br>

`r participant_1_partie` n'ont participé qu'une seule fois...
<br>

En moyenne sur votre site, les participants participent `r round(participant_moy)` fois. Dans le référentiel, en moyenne, les participants réalisent `r round(participant_moy_ref)` parties.
<br>


Répartition des participants selon leur niveau :
```{r}
plot_niv_participant
```
<br>


<font size= "5" >
Pourquoi s'impliquer sur le long terme dans le programme :
</font>

<br>

Vos connaissances s'améliorent et votre niveau progresse :
```{r}
func_diag_radar_chang_niv()
```


<br>
La connaissance de la biodiversité locale s'améliore
```{r}
plot_richesse_cum_partie
```



la participaticipation au cours de la dernière saison
```{r}
plot_pheno_partic_saison
```





# **Les pressions qui s'exercent sur votre site **
```{r lien faire diagramme ternaire sur R}
# packages = c('ggtern', 'plotly', 'readr', 'dplyr', 'tidyr')
# https://rpubs.com/tskam/ternary_plot
# ggtern(data = , aes(x = %urbain,y = %rural, z = %semi-naturel)) + geom_point(coord site)
```